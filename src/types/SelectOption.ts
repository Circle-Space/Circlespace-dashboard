export type SelectOption = {
  value: any;
  label: string;
  description?: string;
};

export type Deviceoption = {
  Id: string;
  DeviceType: string;
  DeviceName: string;
  SerialNumber: string;
  PhoneNumber: string;
  ConnectionString: string;
  IsActive: boolean;
};